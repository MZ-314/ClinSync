# Base URL of the ClinSync backend API.
# Local development against `uvicorn` running on port 8000:
VITE_API_URL=http://127.0.0.1:8000

# On Vercel, set this in Project Settings → Environment Variables
# to the deployed Render backend, e.g.:
# VITE_API_URL=https://clinsync-backend.onrender.com
